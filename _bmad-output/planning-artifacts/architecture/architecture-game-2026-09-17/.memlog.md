---
scope: mcweb（我的世界-网页复刻版）纯静态体素沙盒教育游戏平台——回溯性架构脊线（initiative 高度，覆盖全部 E1~E7 现役能力与 E8~E11 前瞻方向的影响面）
purpose: build-substrate
altitude: initiative
source_input: PRD prd-game-2026-09-17 + UX DESIGN/EXPERIENCE + brief addendum + AGENTS.md + docs/ 契约（brownfield 追认既有约定）
updated: 2026-09-17T02:10
---

- (event) run 开始：headless create（brownfield 追认），输入=PRD/UX/addendum+AGENTS.md+docs 契约，语言=简体中文
- (decision) AD-1 纯静态零构建形态 [ADOPTED]：无构建工具/无包管理器/原生 ESM+Three.js CDN/唯一入口 index.html；写文件接口 POST /api/file 公网必须禁用
- (decision) AD-2 单例 state 唯一数据源 [ADOPTED]：全部可变状态挂 js/state.js 导出的 state，模块直接 import 读写，无事件总线/不可变数据
- (decision) AD-3 常量集中在 config.js [ADOPTED]：调参/配方表/BlockInfo 挖掘属性/outline 选取形状全在 config.js 纯数据
- (decision) AD-4 有状态方块状态编进方块 ID [ADOPTED]：编解码纯函数在 config.js，派生态走运行时 Map 读档重算，存档格式零改动=流水线准绳#1；新贴面方块须全套登记（BlockInfo/outline/tile/isPropBlock/getPropMesh/pickBlockItem）防读档丢网格
- (decision) AD-5 事件触发全量重算求解骨架 [ADOPTED]：redstone/kinetic 照同一骨架（事件→全图重算→派生态落 Map）；redstone.js else-if 链禁延长；帧末聚合/局部守卫（仅投水才动力重算、5×5×5 邻接才红石重算、电梯跨格零重算）
- (decision) AD-6 渐进施工队列 [ADOPTED]：AI 建造经 buildQueue 分帧放置+分帧重建；切世界/进关必 clearBuildQueue（幽灵建筑防护 W13）
- (decision) AD-7 存档版本化与迁移 [ADOPTED]：六槽 localStorage RLE+v4 dims 尺寸迁移（种子重生成+老区域逐格覆盖零丢失）；新增持久数据优先 IndexedDB（mcweb-levels），localStorage 限既有 key 与 <100KB 小体积
- (decision) AD-8 关卡卡契约 mcweb.level.v1 [ADOPTED]：schema 冻结只增不改；卡内一律局部坐标，worldToLocal/localToWorld 唯一实现在 levelWorkshop；EMBED_OFFSET{80,4,80}；上限 96×64×96；题目全量拷贝防题库漂移
- (decision) AD-9 模式守卫+存档防线 [ADOPTED]：闯关/编辑一切限制=state.levelRun/levelEdit 判定的 guard，playerPhysics 零改动，拒绝必带文案；saveGame() 入口闸四通道拒写；唯一防线裁决必须穷举失效前提（W 批次 P0 教训）
- (decision) AD-10 uiModal 指针锁唯一管理者 [ADOPTED]：全游戏只有 uiModal.js 调 requestPointerLock/exitPointerLock；互斥状态机+登记的非暂停浮层标志；新浮层一律接入
- (decision) AD-11 AI 助手与游戏本体解耦 [ADOPTED]：assistant/boot.js 零依赖引导、独立侧栏不进状态机、浏览器直连 OpenAI 兼容上游 Key 不出本机；热重载经 snapshot RLE 快照，闯关/编辑激活只落标记恢复即弃（W22）
- (decision) AD-12 确定性模式 [ADOPTED]：worldSeed 进存档混入全部噪声（地形公式只依赖坐标与种子=扩容迁移根基）；官方关卡生成器确定性输出禁手改 JSON；教育锁同格同题格哈希（题不进存档）
- (decision) AD-13 双服务器等价 [ADOPTED]：server.py 与 server-rust/ 接口/JSON/SSE 完全一致；新增 /api 路由必须双实现同步或明确标注仅本机开发（如 /codex 反代）；静态响应 no-store
- (decision) AD-14 部署与缓存红线 [ADOPTED]：主站 submodule apps/mcweb+CI rsync；HTML/JS/CSS no-cache(ETag 304)/媒体 1h；公网 --no-api 或鉴权；HTTPS 下 LLM 上游必须 HTTPS
- (decision) AD-15 内容/代码接口契约化 [ADOPTED]：题库契约 docs/edu-quiz-banks-contract.md=唯一接口+validate_banks.py 执行；关卡模块 API 契约 edu-workshop-impl-contract.md；生成器模块注册制（level_*.mjs 一关一文件）
- (constraint) 技术债1 元件索引：红石/动力全图线性扫描随规模线性放大；触发=世界再扩容或再叠加扫描分支（L2 P01 已部分命中）；PRD NFR-4
- (constraint) 技术债2 存储配额：6 槽×~880KB 逼近 localStorage 5MB；512²+ 扩容或题库铺量前须改「存档种子+玩家改动差分」；PRD NFR-5；关卡卡/缩略图入 IndexedDB（N≤50）
- (constraint) 性能新常态基线（org-log L2 G3 记录值）：红石重算 3.5~3.7ms 门 4.3 / p95 门 4.4 / 动力重算 ≤2.5 / tick mean 门 0.58；数值以 run_l1.py 脚本注释为准
- (version) JavaScript 原生 ES Modules（无框架）/ Three.js 0.160.0（CDN unpkg）/ Python3 标准库 server.py / Rust 标准库 server-rust（零 crate）/ node:test 工具测试 + 自研 CDP 注入 E2E
- (assumption) A-1 本文以 2026-09-16 止代码为基线（与 PRD/UX 同口径）；A-2 性能门数值引用 2026-09-06 org-log 记录值，以脚本内注释为最新；A-3 方块 ID 段位表据 AGENTS.md 与工坊契约整理，空闲段位以 config.js 现状为准；A-4 server-rust 不含 /codex 反代
- (question) OQ-1 教学卡片样式是否抽公共样式表（DESIGN OQ-D3）；OQ-2 元件索引立项时点（下批叠加扫描分支即应立项）；OQ-3 星辉门随卡导出自包含方案未定；OQ-4 E8 新锁具「四件套登记」如何机械化校验；OQ-5 Three.js 经 unpkg CDN 单点依赖是否自托管回退；OQ-6（承 PRD OQ-1）家长导出统计文件格式未定
- (direction) E8~E11 影响面已评估（只标影响面不展开）：E8 新锁具四件套+卡 schema lockType 只增；E9 低段模板/大字模式、高段 kineticStatusAt 报表+rules 扩展；E10 banks.json 多版本 key+生成器矩阵+列表筛选；E11 条件项（FR-41 导出/FR-42 改名，格式字符串冻结）
- (event) spine 起草：ARCHITECTURE-SPINE.md 写出（中文，15 AD + 约定 + Stack + 结构种子 + 能力映射 + 债务 + E8~E11 影响面 + 不变量清单 + Deferred + 假设与开放问题）
- (event) Reviewer Gate（无子代理环境，顺序执行）：lint 0 findings；three@0.160.0 经 npm registry 核验；对抗镜头 3 缝已收紧（AD-5 解锁回写接线/约定 localStorage key 命名登记/约定新模块可测性声明）；rubric 走查补运营面一句（发布门+回退链）；三份评审落 reviews/
- (event) spine finalized（status: final，2026-09-17）
